import QRCode from 'qrcode';
export default async function handler(req,res){try{const u=new URL(req.url,`https://${req.headers.host||'localhost'}`),upi=u.searchParams.get('upi')||'',name=u.searchParams.get('name')||'Video Selling',amount=Number(u.searchParams.get('amount')||0);if(!upi||!amount)return res.status(400).send('UPI and amount are required.');const uri=`upi://pay?pa=${encodeURIComponent(upi)}&pn=${encodeURIComponent(name)}&am=${encodeURIComponent(amount.toFixed(2))}&cu=INR`;const png=await QRCode.toBuffer(uri,{type:'png',width:480,margin:2,errorCorrectionLevel:'M'});res.setHeader('Content-Type','image/png');res.setHeader('Cache-Control','no-store');return res.status(200).send(png)}catch{return res.status(500).send('Unable to generate QR.')}}